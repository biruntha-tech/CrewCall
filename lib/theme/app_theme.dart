import 'package:flutter/material.dart';

class AppColors {
  static const Color primary = Color(0xFFFE5740); // Highlight Orange
  static const Color background = Color(0xFFFFFFFF); // White
  static const Color border = Color(0xFF7C7C7C); // Button/Text Fields Border
  static const Color unselectedChip = Color(
    0xFFF3F4F6,
  ); // Unselected pill background
  static const Color selectedChip = Color(
    0xFFFFF7ED,
  ); // Selected pill background
  static const Color selectedText = Color(
    0xFF9A3413,
  ); // Selected border and text
}
